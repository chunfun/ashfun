module ashman{

    /**
     * System
     */
    class System {
        /**
         * manage the list of systems within the engine. The previous system in the list.
         */
        previous : System;
        /**
         * manage the list of systems within the engine. The next system in the list.
         */
        next : System;

        constructor(parameters) {
            
        }
    }
}